const intialstate = {
    logs:null,
    current:null,
    loading:false,
    errors:null
}

export default (state=intialstate, action) => {
    switch(action.type){
        default: 
        return state;
    }
}